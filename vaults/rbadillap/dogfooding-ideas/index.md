# `dogfooding` ideas

> just ideas, not necessarily implemented but good opportunity to do it when convenient or even necessary

1. ~~syner uses subagent `code-review.md` exactly with the same events as our current `.github/workflows/claude.yml` and mentions @claude when necessary with a proper feedback~~ **Done**: `/syner` skill now calls `code-reviewer` subagent in Phase 3 (Verify)
